SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  f303b2a  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0043_notifications_pushed_at   (unchanged — NO new migration)

Change in scope (backend-only, single commit; NO migration, NO OpenAPI/relay change):
  f303b2a  Deep-link Splitwise activity rows to their expense
    - client.py normalizers now capture the notification's source ({type, id}) as
      source_type / source_id.
    - importer.sync_notifications resolves an Expense-source notification's source_id to the local
      Expense (by splitwise_expense_id) and stamps Notification.entity_type='expense' / entity_id;
      on-conflict backfills; the individual Splitwise push carries the matching deep-link target.
    - tests/test_notifications_sync.py: new test_splitwise_expense_source_deep_links_to_local_expense
      (+ source_type/source_id on _note).

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> f303b2a (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.
  - alembic current -> 0043_notifications_pushed_at (head); no migration applied.

MIGRATION: none (head 0043 unchanged).
RELAY:     UNTOUCHED  (git diff relay/ empty).
OPENAPI:   UNCHANGED  (git diff ios/openapi.json empty).

IMPORT / WIRING SANITY (api-test):
  - app.main + splitwise client/importer import clean.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 228   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notifications_sync ...... 10  (incl. NEW
                                       test_splitwise_expense_source_deep_links_to_local_expense,
                                       test_sync_notifications_all_per_token_isolation)

FAILURES: none.

SUMMARY: PASS. f303b2a builds, restarts, and passes the full backend suite (228/228) on the running
stack. Migration head 0043 (untouched); no relay/OpenAPI change. Splitwise expense-source deep-linking
verified: source_type/source_id captured, resolved to the local Expense by splitwise_expense_id,
Notification entity stamped + push deep-link target. No Linux-side code changes; this file is the only
commit from here.
