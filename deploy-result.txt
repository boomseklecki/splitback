SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  fa61807  (main HEAD)
Date (UTC):     2026-07-01
Migration head: 0049_drop_categories_blob   (unchanged — NO migration)

Change in scope (backend-only; iOS ships separately):
  fa61807  Splitwise push: map Fees->Taxes, Travel->Hotel; drop asymmetric Subscriptions alias
    - app/integrations/splitwise/mapper.py: _CATEGORY_ALIASES gains "fees"->"taxes" and
      "travel"->"hotel"; the old Subscriptions->"tv/phone/internet" alias is removed so Subscriptions
      now pushes to Splitwise as "General" (intended — it's a cross-cutting app concept, not a
      Splitwise category).
    - tests/test_splitwise_extra.py::test_resolve_category_id updated to cover this.
    - (all other files in the commit are iOS — ship separately, no server action.)

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> fa61807 (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real prod, :8000) + db-test/api-test. Up.
  - alembic current -> 0049_drop_categories_blob (head); no migration applied.

MIGRATION: none (head 0049 unchanged). No DB schema change.

FUNCTIONAL CHECK (resolve_category_id against a Splitwise name->id taxonomy):
  - Fees ........... -> Taxes    (via new "fees"->"taxes" alias)
  - Travel ......... -> Hotel    (via new "travel"->"hotel" alias)
  - Subscriptions .. -> General  (unmapped by design; Subscriptions alias removed)

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 258   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_splitwise_extra ..... 14  (incl. updated test_resolve_category_id)

FAILURES: none.

SUMMARY: PASS. fa61807 builds, restarts, and passes the full backend suite (258/258) on the running
stack. No migration, no schema change. Splitwise push category mapping verified functionally:
Fees->Taxes, Travel->Hotel, Subscriptions->General (unmapped, intended). iOS ships separately. This
file is the only commit from here.
