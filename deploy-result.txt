SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  d620809  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0043_notifications_pushed_at   (NEW — upgraded 0042 -> 0043)

Change in scope (backend + iOS, single commit; NO relay change, NO OpenAPI change):
  d620809  Inbox refinements: fix churning push (watermark), All-Activity screen, transaction deep-link
    - migration 0043_notifications_pushed_at (down_revision 0042_notification_entity): adds
      splitwise_tokens.notifications_pushed_at (push watermark). Revision id 28 chars (<=32 OK).
    - importer.sync_notifications: push churn fix — push only notifications newer than the per-token
      watermark, advance it every sync (decoupled from the retention prune). Softened summary wording.
    - tests/test_notifications_sync.py reworked for the watermark (incl. a no-repush regression test).
    - iOS-only: ActivityView (All-Activity screen) + transaction deep-link target.

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> d620809 (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.

MIGRATION:  APPLIED on BOTH databases
  - REAL api db:  0042_notification_entity -> 0043_notifications_pushed_at  (head: 0043_notifications_pushed_at)
  - TEST db:      0042_notification_entity -> 0043_notifications_pushed_at  (head: 0043_notifications_pushed_at)
    "splitwise_tokens.notifications_pushed_at (push watermark)"

RELAY:    UNTOUCHED  (git diff relay/ empty).
OPENAPI:  UNCHANGED  (git diff ios/openapi.json empty).

IMPORT / WIRING SANITY (api-test):
  - app.main imports clean.
  - SplitwiseToken model carries notifications_pushed_at column.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 227   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notifications_sync ...... 9   (reworked for the watermark; incl.
                                       test_resync_updates_content_without_repush no-repush regression,
                                       test_sync_notifications_all_per_token_isolation)

FAILURES: none.

SUMMARY: PASS. d620809 builds, migrates 0042 -> 0043 on both real and test DBs, restarts, and passes
the full backend suite (227/227). Push-churn fix verified: per-token notifications_pushed_at watermark
column + watermark-gated push (no-repush regression green). No relay change, no OpenAPI change. No
Linux-side code changes; this file is the only commit from here.
