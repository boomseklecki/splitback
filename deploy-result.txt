SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  a68d73f  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0040_pending_txn_id  (unchanged — no new migration)

Changes in scope (backend-only):
  dc099fb  OFX import branding from Intuit FIDIR: scripts/refresh_fidir.py +
           committed institutions_data.json (read at runtime; no live Intuit fetch);
           institutions.py rewritten (curated overrides + exact-name match); statements.py wiring;
           new tests/test_institutions.py + extended test_statement_import.py.
  d87f20f  Supported-banks directory: GET /institutions?q=&limit= (routers/institutions.py +
           schemas/institution.py), registered under _protected.
  a68d73f  OFX parser: smarter NAME/MEMO merchant description (banks that flip the fields);
           parser-only ofx.py change + 4 new test_ofx_parser cases.

BUILD / RESTART:  OK
  - git pull --ff-only to a68d73f (clean).
  - docker compose build api api-dev api-demo api-test -> built (institutions_data.json, 844 KB, in image).
  - Recreated api (real, :8000), api-dev, api-demo, db-test+api-test. All healthy.
  - alembic upgrade head -> no-op (already 0040_pending_txn_id).
  - import sanity: app.main + institutions/statements routers import clean.

FEATURE WIRING (verified):
  - GET /institutions ............ present in live OpenAPI spec
  - FIDIR dataset loads .......... resolve_domain("Chase") -> chase.com (committed JSON read at runtime)

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 212   FAIL: 0   (47 modules, all green)

Watch-list (all PASS):
  test_ofx_parser ...... 12  (incl. the 4 new NAME/MEMO-flip cases)
  test_institutions .... 7   (curated overrides + exact-name match into the FIDIR dataset)
  test_statement_import  green (extended)

FAILURES: none.

SUMMARY: PASS. a68d73f builds, restarts, and passes the full backend suite (212/212) on the running
stack. Migration head 0040 (untouched). No live-fetch dependency (institutions_data.json committed +
read at runtime). No Linux-side code changes; this file is the only commit from here.
