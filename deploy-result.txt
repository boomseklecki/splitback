SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  2def084  (main HEAD)
Date (UTC):     2026-06-30
Migration head: 0049_drop_categories_blob   (NEW — upgraded 0048 -> 0049)

Change in scope (backend + iOS-ships-separately, single commit):
  2def084  Phase 5: retire the legacy categories.v1 preferences blob
    - 0049_drop_categories_blob (down_revision 0048_goal_budget_notify): single statement
      DELETE FROM user_preferences WHERE key = 'categories.v1'. Touches NO other key and NOT
      spend_categories/category_maps. Downgrade no-op by design. Rev id 25 chars (<=32 OK).
    - models/user_preference.py, routers/preferences.py: blob retired from the read/write path.
    - new tests/test_categories_backfill.py (test_categories_blob_retired_0049).

SAFETY:
  - Pre-migration pg_dump of the prod DB captured before this destructive DELETE (2.66 MB, exit 0),
    held in the session scratchpad as a rollback reference.

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> 2def084 (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real prod, :8000) + db-test/api-test. Up.

MIGRATION:  APPLIED on BOTH databases (0048 -> 0049)
  - REAL prod db:  head 0049_drop_categories_blob
  - TEST db:       head 0049_drop_categories_blob

PROD DATA VERIFICATION (before -> after):
  - categories.v1 rows ............... 1 -> 0   (deleted, as intended)
  - other user_preferences rows ...... 3 -> 3   (UNCHANGED — survivors intact)
  - surviving keys ................... goalsOrder.v1, suggestions.v1, tabOrder.v1  (all present)
  - spend_categories rows ............ 24 -> 24  (UNCHANGED — relational taxonomy from 0047 intact)
  - category_maps rows ............... 0 -> 0    (UNCHANGED — owner had no custom raw->canonical maps)
  Confirmed: zero categories.v1 rows remain; spend_categories/category_maps counts unchanged; no other
  preference key touched.

BUDGET PUSH GATE (left dormant per instruction — NOT enabled):
  - Authoritative resolve on prod: server_settings.get(... 'budget_push_enabled') -> False. Untouched.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 258   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_categories_backfill ..... 2   (incl. test_categories_blob_retired_0049)

FAILURES: none.

SUMMARY: PASS. 2def084 builds, migrates 0048 -> 0049 on both real prod and test DBs, restarts, and
passes the full backend suite (258/258). 0049 deleted the single categories.v1 blob on prod and nothing
else: other preference keys (goalsOrder.v1, suggestions.v1, tabOrder.v1) and the relational category
data (spend_categories 24, category_maps 0) are unchanged. budget_push_enabled remains OFF. Pre-migration
prod pg_dump captured. iOS ships separately. This file is the only commit from here.
