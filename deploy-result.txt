SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  1cbf0d7  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0040_pending_txn_id  (unchanged — no new migration this round)

Main change in scope:
  1cbf0d7  Splitwise download-all receipts: gated bulk background button +
           scheduled auto-backfill; new splitwise_receipt_backfill_enabled setting
  (40ef73d, 60f1cc2, 95c6b10 are iOS-only.)

BUILD / RESTART:  OK
  - git pull --ff-only to 1cbf0d7 (clean).
  - docker compose build api api-dev api-demo api-test -> built.
  - Recreated api (real, :8000), api-dev, api-demo, db-test+api-test. All healthy.
  - alembic upgrade head -> no-op (already 0040_pending_txn_id).
  - import sanity: app.main + splitwise/sync/receipts import clean.

FEATURE WIRING (verified):
  - POST /splitwise/receipts/download-all .............. present in live OpenAPI spec
  - POST /splitwise/groups/{group_id}/download-receipts  still present
  - ServerSettingsResponse.splitwise_receipt_backfill_enabled .. present (GET/PATCH round-trip OK)
  - server_settings.REGISTRY has splitwise_receipt_backfill_enabled .. yes
  - services.sync.backfill_splitwise_receipts .......... present (hooked into sync_all, per-token, gated)

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 197   FAIL: 0   (45 modules, all green)

Watch-list (all PASS):
  test_receipt_backfill ..... 1  (pending_receipt_expense_ids ordering/filters: newest-first,
                                   excludes already-receipted / no-URL / out-of-scope)
  test_server_settings ...... 4  (incl. the new splitwise_receipt_backfill_enabled round-trip)
  test_splitwise_extra ...... 14
  test_splitwise_scoped_sync . 4

FAILURES: none.

NOTE: the live Splitwise receipt *fetch* path (download-all worker hitting Splitwise + MinIO) needs a
real token + network and is not exercised by the suite (the unit/DB/HTTP tests cover ordering, filters,
gating, endpoint registration, and the settings round-trip). On prod the setting is currently OFF and the
receipts-prod bucket holds 0 receipt objects (753 expenses still have only their remote splitwise_receipt_url).

SUMMARY: PASS. 1cbf0d7 builds, restarts, and passes the full backend suite (197/197) on the running
stack. Migration head 0040 (untouched). No Linux-side code changes; this file is the only commit from here.
