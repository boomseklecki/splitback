SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  27c2271  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0044_notification_hidden   (unchanged — NO new migration)

Change in scope (backend-only, single commit; NO migration, NO OpenAPI/relay/iOS change):
  27c2271  Deep-link Group-source Splitwise notifications
    - importer.sync_notifications: extends the source resolution — source.type 'Group' now resolves
      to a local Group by splitwise_group_id and stamps entity_type='group' / entity_id (the iOS
      .group target already exists). Mirrors the expense-source resolution.
    - tests/test_notifications_sync.py: new test_splitwise_group_source_deep_links_to_local_group.

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> 27c2271 (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.
  - alembic current -> 0044_notification_hidden (head); no migration applied.

MIGRATION: none (head 0044 unchanged).
RELAY:     UNTOUCHED  (git diff relay/ empty).
OPENAPI/iOS: UNCHANGED  (git diff ios/ empty; .group target already existed).

IMPORT / WIRING SANITY (api-test):
  - app.main + splitwise importer import clean.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 232   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notifications_sync ...... 12  (incl. NEW
                                       test_splitwise_group_source_deep_links_to_local_group,
                                       test_sync_notifications_all_per_token_isolation)

FAILURES: none.

SUMMARY: PASS. 27c2271 builds, restarts, and passes the full backend suite (232/232) on the running
stack. Migration head 0044 (untouched); no relay/OpenAPI/iOS change. Group-source deep-linking
verified: source.type 'Group' resolves to the local Group by splitwise_group_id and stamps
entity_type='group'/entity_id (mirrors the expense-source path). No Linux-side code changes; this file
is the only commit from here.
