SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  a95aeb2  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0040_pending_txn_id  (unchanged — no new migration)

Change in scope (backend-only):
  a95aeb2  OFX<->Plaid duplicate guard.
    - app/routers/statements.py: import_ofx gained force=False + a pre-create guard;
      POST /statements/import gained a `force` query param.
    - app/schemas/statement.py: StatementImportResult.plaid_conflict: bool = False.
    - tests/test_statement_import.py: new test_plaid_linked_card_guards_then_forces.

BUILD / RESTART:  OK
  - git pull --ff-only to a95aeb2 (clean).
  - docker compose build api api-dev api-demo api-test -> built.
  - Recreated api (real, :8000), api-dev, api-demo, db-test+api-test. All healthy.
  - alembic upgrade head -> no-op (already 0040_pending_txn_id).
  - import sanity: app.main + statements router import clean; StatementImportResult.plaid_conflict present.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 200   FAIL: 0   (46 modules, all green)

Watch:
  test_statement_import ...... 6  (incl. NEW test_plaid_linked_card_guards_then_forces:
                                   OFX import of a Plaid-linked card returns plaid_conflict, then
                                   ?force= proceeds)

FAILURES: none.

SUMMARY: PASS. a95aeb2 builds, restarts, and passes the full backend suite (200/200) on the running
stack. Migration head 0040 (untouched). No Linux-side code changes; this file is the only commit from here.
