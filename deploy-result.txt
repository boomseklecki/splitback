SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  8b1789a  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0040_pending_txn_id  (unchanged — no new migration)

Change in scope (backend + iOS contract, single commit):
  8b1789a  Splitwise: fast notifications-only poll for near-real-time partner-activity push
    - server_settings.py: new key notifications_poll_minutes (int, default 0); added to
      ServerSettingsResponse + ServerSettingsUpdate schemas.
    - services/notifications_scheduler.py (new): 60s-tick scheduler (run_scheduler), launched in
      main.py lifespan (off unless notifications_poll_minutes > 0).
    - services/sync.py: new sync_notifications_all; both splitwise loops now iterate detached value
      tuples + UPDATE the cursor (fixes a latent multi-token MissingGreenlet on rollback).
    - importer.sync_notifications: dropped `limit` param -> fetch min(retention, 100); cold-start
      push guard.
    - Regenerated ios/openapi.json + iOS ServerSettingsView control.

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> 8b1789a (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.
  - alembic current -> 0040_pending_txn_id (head); no migration applied.

STARTUP (api logs): CLEAN
  - "Application startup complete." / Uvicorn running; no tracebacks.
  - main.py lifespan now launches 4 background tasks:
      run_backup_scheduler, run_sync_scheduler, run_notifications_scheduler (NEW), run_demo_prune_scheduler.
    notifications_scheduler is a no-op unless notifications_poll_minutes > 0.

IMPORT / WIRING SANITY (api-test):
  - app.main imports clean.
  - notifications_scheduler.run_scheduler present.
  - sync.sync_notifications_all present.
  - importer.sync_notifications signature: (session, client, owner_identifier, retention,
                                            access_token, push)   [`limit` dropped]
  - server_settings schemas: notifications_poll_minutes present in BOTH Response and Update.

CONTRACT (presence-check, per flattened-nullables memory):
  - notifications_poll_minutes present in committed ios/openapi.json AND the live /openapi.json.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 219   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notifications_sync ...... 7   (incl. test_sync_notifications_all_per_token_isolation)
  test_notifications ........... 4
  test_notification_producer ... 4
  test_splitwise_auth .......... 3
  test_splitwise_scoped_sync ... 4
  test_server_settings ......... 4

FAILURES: none.

SUMMARY: PASS. 8b1789a builds, restarts with clean startup (4 background tasks incl. the new
notifications poll), and passes the full backend suite (219/219). Migration head 0040 (untouched).
New fast-poll surface verified: notifications_poll_minutes setting, run_scheduler, sync_notifications_all,
sync_notifications limit-drop. Contract aligned. No Linux-side code changes; this file is the only
commit from here.
