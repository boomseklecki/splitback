SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  dec82dd  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0041_notification_prefs   (NEW — upgraded 0040 -> 0041)

Change in scope (backend + iOS contract, single commit):
  dec82dd  Notification preferences: per-user push/feed mute (two channels) + link-sensitivity sync
    - migration 0041_notification_prefs (down_revision 0040_pending_txn_id): new notification_mutes
      table (owner_identifier, token). Revision id 23 chars (<=32 OK).
    - new model app/models/notification_mute.py (NotificationMute).
    - router app/routers/notification_mutes.py (GET/PUT /notification-prefs), registered under
      _protected in main.py; schema app/schemas/notification_mute.py.
    - notify.py: push_muted_owners() — feed row written for ALL, push only to non-push-muted.
    - importer.sync_notifications: drops push for push:source:splitwise / push:<type> mutes.
    - regenerated ios/openapi.json + iOS (NotificationSettingsView, prefs cache,
      link-sensitivity blob sync).

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> dec82dd (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.

MIGRATION:  APPLIED on BOTH databases
  - REAL api db:  0040_pending_txn_id -> 0041_notification_prefs  (head: 0041_notification_prefs)
  - TEST db:      0040_pending_txn_id -> 0041_notification_prefs  (head: 0041_notification_prefs)
    "notification_mutes (per-owner push/feed preferences)"

IMPORT / WIRING SANITY (api-test):
  - app.main imports clean.
  - models.notification_mute.NotificationMute present.
  - services.notify.push_muted_owners present.

CONTRACT / ROUTE (presence-check, per flattened-nullables memory):
  - GET/PUT /notification-prefs present in live /openapi.json AND committed ios/openapi.json.

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 223   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notification_prefs ...... 2   (test_put_then_get_roundtrips_and_replaces)
  test_notification_producer ... 5   (extended: push-mute keeps feed row, drops push)
  test_notifications_sync ...... 8   (extended: splitwise push-mute)

FAILURES: none.

SUMMARY: PASS. dec82dd builds, migrates 0040 -> 0041 on both real and test DBs, restarts, and passes
the full backend suite (223/223). New per-user notification-prefs surface verified: notification_mutes
table, NotificationMute model, GET/PUT /notification-prefs route, push_muted_owners two-channel mute
(feed-for-all, push-for-unmuted), splitwise push-mute drop. Contract aligned. No Linux-side code
changes; this file is the only commit from here.
