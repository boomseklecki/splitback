SplitBack backend deploy verification (uplink / Linux full-stack)
================================================================

Commit tested:  6ae2b96  (main HEAD)
Date (UTC):     2026-06-29
Migration head: 0040_pending_txn_id  (unchanged — no new migration)

Change in scope (backend-only, single commit):
  6ae2b96  Splitwise notifications: connect-time backfill, scheduler sync + delta push, HTML cleanup
    - client.py: fetch_notifications gains `limit`; _clean_content strips HTML in both normalizers.
    - importer.sync_notifications: `limit` + `push` flag (delta push of new non-self notifications).
    - splitwise_auth callback: connect-time backfill of last 20 (push=False).
    - sync.py scheduler: per-token notification sync after expense commit (push=True), isolated.
    - new tests/test_notifications_sync.py; test_splitwise_auth + test_splitwise_scoped_sync updated
      for the new fetch_notifications arity.

BUILD / RESTART:  OK
  - git merge --ff-only origin/main -> 6ae2b96 (clean).
  - docker compose build api api-test -> built.
  - Recreated api (real, :8000) + db-test/api-test. Up.
  - alembic current -> 0040_pending_txn_id (head); no migration applied.

IMPORT / WIRING SANITY (api-test):
  - app.main imports clean.
  - client.fetch_notifications signature: (client, access_token, limit)        [new `limit`]
  - client._clean_content present
  - importer.sync_notifications signature: (session, client, owner_identifier, retention,
                                            access_token, limit, push)          [new `limit`, `push`]

TEST RUN (api-test stack, incl. HTTP/MinIO-dependent tests):  PASS
  TOTAL passing: 216   FAIL: 0   (all modules green)

Watch-list (all PASS):
  test_notifications_sync ...... 4   (incl. test_push_only_new_non_self,
                                       test_resync_updates_content_without_repush)
  test_notifications ........... 4
  test_notification_producer ... 4
  test_splitwise_auth .......... 3   (incl. test_start_binds_state_to_caller)
  test_splitwise_scoped_sync ... 4   (new fetch_notifications arity)

FAILURES: none.

SUMMARY: PASS. 6ae2b96 builds, restarts, and passes the full backend suite (216/216) on the running
stack. Migration head 0040 (untouched). New notification-sync surface verified: fetch_notifications/
sync_notifications gained limit+push, connect-time backfill + scheduler delta-push wired. No Linux-side
code changes; this file is the only commit from here.
